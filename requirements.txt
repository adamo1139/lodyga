# Renderowanie szablonu czatu w generate_answers.py
transformers>=4.55
huggingface_hub>=0.30

# Detekcja języka w aggregate_lodyga.py (OpenLID v3, ~1,2 GB, pobierany raz
# z cache'u Hugging Face). Można pominąć flagą --no-language-detection.
fasttext>=0.9.2
