gradio==4.31.4
plotly==5.18.0
# Needed by generate_answers.py (the upstream browser requirements are above).
transformers>=4.55
huggingface_hub>=0.30

# Language identification for the descriptive Polish-share statistic
# (aggregate_lodyga.py). The OpenLID v3 model is ~1.2 GB and is fetched from the
# Hugging Face cache on first use; pass --no-language-detection to skip it.
fasttext>=0.9.2
